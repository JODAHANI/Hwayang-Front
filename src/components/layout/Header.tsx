import * as React from 'react';

const Header = (): JSX.Element => {
  return (
    <header className='border-light-blue-500 fixed flex h-8 w-full border-b border-solid bg-white'></header>
  );
};

export default Header;
